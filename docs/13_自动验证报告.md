# ECHO Mind Path A v0.2 自动验证报告

执行日期：2026-07-29

## 结果摘要

| 验证项 | 结果 |
|---|---|
| Python 源码编译 | 通过 |
| 后端 pytest | **671 passed** |
| 内容包 JSON/字段/哈希校验 | 4 个内容包通过 |
| Path A 宣称词扫描 | 通过 |
| 生产路径动态代码检查 | 通过 |
| 合成安全语料回归 | 650/650 |
| OpenAPI 导出 | 通过 |
| Alembic upgrade/downgrade | 通过（SQLite） |
| Pilot 密钥/数据库预检 | 通过（使用临时强密钥配置） |
| FastAPI HTTP 冒烟 | `/health`、`/ready`、`/console` 均 HTTP 200 |
| Kotlin 领域核心编译 | 通过 |
| Git 完整性与压缩包校验 | 发布打包阶段通过 |

## 后端测试覆盖的关键行为

- 版本化同意及撤回后阻断心理数据处理
- L0 当前危险直接创建 L3 人工事件
- PHQ-9/GAD-7 确定性计分；PHQ-9 高风险题项旁路人工
- 情绪日记创建、修订链、逻辑删除和红色信号识别
- 签到、量表、练习和风险事件的租户内幂等
- 相同 `event_id` 在不同租户间互不冲突
- 用户不能访问其他用户或其他租户数据
- 紧急联系人和签到备注加密存储
- 风险事件必须先人工接管才能关闭，关闭后才能复盘
- 审计哈希链完整性验证
- 数据导出/删除请求工作流
- 安全响应头和请求 ID

## 合成红队语料

`red_team_corpus.v1.jsonl` 共 650 条：

- 直接红色风险：160
- 需人工复核（含否定包装）：190
- 退出指令：50
- 正常负面表达对照：250

当前确定性规则在这组合成语料上 650/650。该结果只证明规则与预设测试集一致，**不证明临床敏感度、真实世界泛化或安全保证**。真实试点前仍须由临床、安全团队扩充方言、多轮语境、错别字和机构真实失效模式，并进行独立红队。

## Android 验证边界

当前容器缺少 Android SDK、模拟器和目标手机，且无法从 `dl.google.com` 下载 SDK，因此未能诚实生成 APK/AAB。已完成：

- Kotlin 纯领域核心（模型、安全规则、量表计分）编译
- Gradle/AGP/Kotlin/KSP 版本兼容基线修正
- Android CI，联网环境将执行 `test assembleDebug lint`
- Room v1→v2 迁移源码、离线 Outbox 和同步契约静态检查

## 未在当前环境执行

- PostgreSQL Docker 集成：当前容器无 Docker 可执行环境
- Android 全量构建、模拟器、UI 自动化和真机矩阵
- KMS/HSM、机构 IAM/MFA、真实通知通道
- 外部渗透测试、独立红队、值班演练和真实用户试点

这些项目已形成可签署的外部任务清单，不能被虚假标记为完成。
