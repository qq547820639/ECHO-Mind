# 内容包治理

状态：Draft → Clinical Reviewed → Safety Reviewed → Compliance Reviewed → Signed Release → Retired。

每个内容包必须包含：唯一 ID、版本、适用人群、禁忌、步骤、退出条件、审核人、签名哈希、生效时间、替代版本和回滚说明。

生产客户端只接受签名包；签名验证失败时拒绝加载并使用上一个已批准版本。
